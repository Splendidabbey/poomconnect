# OS & IDE
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Dependencies
/vendor/
composer.phar

# Test artifacts
/.phpunit.cache
/tests/tmp/

# Local secrets (never commit)
config/database.local.php
.env
.env.local

# Uploads (keep structure, ignore user content)
uploads/slips/*
uploads/events/*
uploads/logos/*
uploads/og/*
!uploads/og/.gitkeep
!uploads/slips/.gitkeep
!uploads/events/.gitkeep
!uploads/logos/.gitkeep

# Seed script should not exist on production after setup
# (still tracked in repo for initial deploy — remove on server manually)

# FTP deploy state (lives on the server; ignore if downloaded locally)
.ftp-deploy-sync-state.json

# Logs
*.log
poomconnect_images/app-brief